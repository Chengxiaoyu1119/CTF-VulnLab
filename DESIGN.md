# VulnLab 设计系统

## 设计方向

VulnLab 是本地安全训练工作台，视觉方向是“精密安全实验工作台”，不是后台管理模板。界面优先帮助用户快速识别靶场、确认运行条件并启动环境，装饰只在能够强化层级或状态时出现。

## 产品契约

### 核心任务

VulnLab 的主任务是把本机资源变成可进入的练习环境：

```text
选择靶场 → 确认状态 → 安装资源 → 准备依赖 → 启动环境 → 打开页面 → 停止环境
```

每一次页面调整都要优先回答两个问题：用户当前处于哪一步、下一步唯一主操作是什么。视觉变化只有在帮助用户识别状态、减少决策或确认结果时才保留。

### 页面边界

| 页面 | 任务 | 主要出口 |
| --- | --- | --- |
| 登录 | 建立本地会话 | 进入靶场 |
| 靶场 | 选择资源并执行下一步 | 安装靶场、查看环境、启动环境、选择机器、打开页面 |
| 运行 | 管理已经启动的实例 | 打开页面、续期、停止 |
| 环境 | 处理依赖、服务参数和项目运行时 | 准备环境、重新检查、保存设置 |

页面只承载自己的任务，目录页不承担介绍页职责，环境页也不重复靶场目录信息。

### 状态到动作

| 状态 | 用户看到的主动作 |
| --- | --- |
| 待安装 | 安装靶场 |
| 安装中 / 排队中 | 显示进度或等待状态 |
| 待配置 | 查看环境 |
| 可启动 | 启动环境 |
| 运行中 | 打开页面 |
| VulnHub 已就绪 | 选择机器 |
| 安装失败 | 重试安装 |

异步操作需要在原位置即时显示进行中状态，并在结束后给出成功或失败反馈。当前操作期间，表单草稿和用户选择保持原样。

### 视觉提示词边界

Apple-inspired 只作为层级、字体、反馈、动效和空间秩序的参考方法；它不是颜色、圆角、留白和阴影的套版指令。固定 3×3 矩阵、Chengxiaoyu ASCII、精简文案和无组件库式侧栏属于 VulnLab 的产品约束，优先级高于通用视觉模板。

## 设计原则

- 保留桌面端 3×3 靶场矩阵和移动端双列矩阵。
- 3×3 矩阵只负责选择靶场；每个卡片直接呈现当前状态和下一步动作，不把目录做成介绍页。
- 让靶场封面成为第一视觉信息，标题和操作贴近内容，不制造空白的组件底栏。
- 交互按真实启动链路表达：待安装 → 安装靶场；可启动 → 启动环境；运行中 → 打开页面 / 停止；待配置 → 查看环境；VulnHub → 选择机器。
- 用留白、对齐、背景层和尺度建立层级，边框只保留给必要的交互边界。
- 使用系统字体保证离线运行和中文可读性；ASCII 与运行日志使用等宽字体形成品牌识别。
- 运行状态使用文字和颜色共同表达含义。

## Design Tokens

### 颜色

| Token | 值 | 用途 |
| --- | --- | --- |
| `--workspace-ink` | `#12333d` | 标题、主要文字 |
| `--workspace-muted` | `#667f86` | 导航、辅助文字 |
| `--workspace-canvas` | `#f5f8f7` | 工作区背景 |
| `--workspace-accent` | `#167f8c` | 启动、打开等主要操作 |
| `--workspace-indigo` | `#5d62b6` | 配置、选择等次级动作 |
| `--workspace-success` | `#188a71` | 就绪、运行中状态 |
| `--workspace-warm` | `#b97828` | 等待、安装中状态 |
| `--coral` | `#c9544d` | 错误、停止和重试 |

### 间距、圆角与字体

- 间距基线：`4 / 8 / 12 / 16 / 20 / 24 / 32px`。
- 圆角层级：控件 `9px`、卡片 `14px`、工作区 `18px`。
- UI 字体：系统无衬线字体，优先 `SF Pro`、`PingFang SC`、`Segoe UI Variable`。
- 运行信息：`ui-monospace` 等宽字体，避免与产品 UI 文字混用。
- 交互控件最小触控高度：桌面端约 `34px`，移动端 `44px`。

## 组件规则

### 靶场矩阵

靶场卡片使用封面作为主体视觉，标题和状态位于统一的顶部信息条，操作位于底部。`ready`、`running`、`importing`、`attention`、`error` 状态通过状态线、状态文字和按钮文字共同表达。状态文字只保留启动决策所需的信息；动作按钮按启动、配置、选择、停止和重试区分视觉语义。

### 产品任务流

页面的核心不是浏览靶场资料，而是把本地资源变成可进入的练习环境：

1. 在靶场矩阵识别资源状态。
2. 按卡片动作安装、配置或启动。
3. 启动成功后从卡片或“运行”页打开真实入口。
4. 在“运行”页续期或停止实例；在“环境”页处理运行时依赖。

VulnHub 需要先选择具体机器，再下载并校验镜像，最后启动 QEMU 实例，因此使用“选择机器”而不是“启动环境”。

### 运行状态栏

左侧保留 Chengxiaoyu ASCII 图形和运行日志，日志仅展示启动决策所需的信息。状态内容使用等宽列对齐，避免增加无实际用途的说明文字。

### 反馈与弹窗

登录反馈和操作反馈采用白色短卡片，支持关闭和自动消失；错误、成功、加载和禁用状态需要给出可理解的文字反馈。

## 响应式规则

- `1440×900`：左侧运行状态栏 + 右侧 3×3 靶场矩阵。
- `768×1024`：运行状态栏缩窄到约 `246px`，靶场矩阵切换为双列，保证英文标题和操作按钮拥有稳定宽度。
- `390×844`：运行状态压缩为顶部信息带，靶场矩阵切换为双列。
- `320～430px`：不产生横向滚动，按钮保持可触控尺寸，封面不变形。

## 验收场景

1. 首次进入靶场页，用户能从卡片标题和主按钮判断下一步操作。
2. 点击安装、准备、启动或下载后，原按钮立即进入对应进行中状态，完成后回到真实状态。
3. 依赖缺失时，卡片进入“待配置”，点击后直达环境页；依赖恢复后返回“启动环境”。
4. VulnHub 先选择机器，再完成镜像下载与校验，最后启动实例；三个阶段各自有明确反馈。
5. 运行页为空时可以回到靶场页，运行中可以打开页面或停止实例。
6. 320px、390px、768px、1440px 下核心按钮、弹窗和表单均可操作，页面无横向溢出。

## 可访问性与性能

- 使用语义化标题、表单标签、ARIA 状态和对话框语义。
- 弹窗支持 `Escape`、焦点回收和 Tab 循环；可聚焦元素保留明显 focus-visible 状态。
- VulnHub 机器列表支持 `↑ / ↓ / Home / End` 移动选择，`Enter` 执行当前操作。
- 封面使用 `loading="lazy"` 和 `decoding="async"`，不新增运行时依赖。
- 动画优先使用 `opacity`、`transform`，并响应 `prefers-reduced-motion`。
