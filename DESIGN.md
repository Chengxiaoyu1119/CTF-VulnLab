# VulnLab 设计系统

## 设计方向

VulnLab 是本地安全训练工作台。页面参考橘子控制台的深色、紧凑、直接反馈语言，不使用后台管理系统的侧边导航，也不把功能堆成多个页面。界面优先帮助用户完成一条真实链路：选中靶场、启动环境、进入练习页面、停止环境。

## 产品契约

| 页面 | 任务 | 主要出口 |
| --- | --- | --- |
| 登录 | 建立本地会话 | 进入靶场 |
| 靶场 | 识别九个内置靶场 | 点击封面打开详情 |
| 详情弹窗 | 管理单个靶场和运行实例 | 启动、打开、续期、停止 |

运行时检查属于启动服务的内部流程，不占用独立页面。服务参数由 API 和部署配置维护。用户不需要选择 Provider、下载镜像或选择机器；每个内置靶场由固定配置自动选择准备器和运行方式。

### 状态到动作

| 状态 | 唯一主动作 |
| --- | --- |
| 待准备 | 启动环境 |
| 准备中 / 排队中 | 显示准备状态 |
| 可启动 | 启动环境 |
| 运行中 | 打开页面 |
| 准备失败 | 重试启动 |

异步操作在原位置显示进行中状态。重复点击、网络失败和 Provider 错误必须保留可理解反馈，不丢失当前详情状态。

## 视觉规则

- 桌面端使用固定 3×3 靶场矩阵，移动端使用双列矩阵。
- 卡片只展示官方封面和底部名称栏，不展示日期、时间、状态、介绍或计数。
- 卡片四边保持统一圆角；默认没有外框和阴影，悬停只做轻微上移和橙色焦点边缘。
- 详情弹窗以封面为首，操作集中在底部，保持信息短、动作明确。
- 页面不使用独立顶部栏、侧边导航、环境管理页或重复的运行管理页。
- 橙色用于主操作和交互焦点，绿色用于就绪与运行，黄色用于等待，红色用于失败和停止。
- 系统字体负责中文可读性，版本、容量和运行地址使用等宽字体。

## Design Tokens

| Token | 值 | 用途 |
| --- | --- | --- |
| `--page` | `#121212` | 页面底色 |
| `--surface` | `#1e1e1e` | 卡片、登录和弹窗表面 |
| `--workspace-canvas` | `#121212` | 靶场工作区 |
| `--workspace-ink` | `#f5f5f5` | 标题和主要文字 |
| `--workspace-muted` | `#a5a5a5` | 辅助文字 |
| `--workspace-accent` | `#ff7f2a` | 主操作和焦点 |
| `--amber` | `#ffb400` | 悬停和等待 |
| `--workspace-success` | `#62b889` | 就绪和运行 |
| `--coral` | `#e05d54` | 错误和停止 |

间距基线为 `4 / 8 / 12 / 16 / 20 / 24 / 32px`。输入框使用 `6px` 圆角，按钮使用 `7～8px`，卡片和弹窗使用 `12～14px`；工作区本身没有圆框、描边或投影。

## 运行链路

```text
点击封面
  -> 详情弹窗
  -> 服务内部准备固定版本源码或官方发行包
  -> 服务内部检查并准备当前靶场需要的依赖
  -> 创建独立运行副本
  -> 启动原生进程
  -> 通过入口打开页面
  -> 续期或停止并回收资源
```

XVWA 使用官方固定 commit `fb30fa517d288e618b521d252f61107ef6a24797`。它与 DVWA、Pikachu、SQLi-Labs、Mutillidae 共用 `native-php` Provider：启动时创建独立 MySQL 资源，初始化数据库，再以 `/xvwa/` 作为实例入口。XVWA 内部练习账号属于靶场本身，不是 VulnLab 主应用账号。

## 响应式验收

- `1440×900`：约 `1120×720px` 工作区，3×3 矩阵居中展示。
- `768×1024`：矩阵切换为双列。
- `390×844`：矩阵保持双列，卡片高度约 `140px`。
- `320px`：不出现横向滚动；按钮、弹窗和输入内容不溢出。

## 可访问性与性能

- 使用语义化标题、表单标签、ARIA 状态和对话框语义。
- 弹窗支持 `Escape`、焦点回收、Tab 循环和 `focus-visible`。
- 封面使用 `loading="lazy"` 与 `decoding="async"`。
- 动画只使用 `opacity` 和 `transform`，响应 `prefers-reduced-motion`。
- 不新增运行时依赖，不把资源、数据库、日志或二进制写进 Git。

## 验收场景

1. 九个固定靶场按种子顺序出现，XVWA 不再显示为 VulnHub。
2. 详情弹窗只给出“启动环境”主动作，不展示安装、配置或额外设置入口。
3. 首次启动由服务自动准备资源和项目运行时；依赖确实不可用时通过启动失败提示反馈原因。
4. 启动成功返回真实实例入口，停止后端口、数据库和副本均回收。
5. 详情和确认弹窗在桌面、平板和手机上不横向溢出。
